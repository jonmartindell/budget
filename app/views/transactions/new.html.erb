<div class="container">
  <% if @transaction.category %>
    <div class="row"> <h3 class="col-xs-12 text-nowrap"><%= @transaction.category.name %> Trans</h3> </div>
  <% else %>
    <div class="row"> <h3 class="col-xs-12 text-nowrap">New  Transaction</h3> </div>
  <% end %>

  <%= render 'form' %>

</div>
